import 'bootstrap/dist/css/bootstrap.min.css';
import { api_query } from './../api/query.betterdoctor.js';

$(document).ready(function () {

	/* * * * * * * * * * * FORM SUBMIT * * * * * * * * * * */
	$('#better-doctor-query').submit(function (event) {

	});
});
